import { BusinessPanel } from "@/components/business/BusinessPanel";

// Iteracja 12: ekran statyczny (mock dane, zgodnie z kryterium roadmapu:
// "Statyczny, kompletny ekran") — taby Agencja/Deweloper w pełni
// interaktywne, modal planów subskrypcji, akcje bez backendu jako toast.
export default function BusinessPage() {
  return <BusinessPanel />;
}
