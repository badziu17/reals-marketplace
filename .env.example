# Faza 0
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/reals?schema=public"

# Iteracja 1 (auth) — uzupełnić przed startem tej iteracji
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""          # wygeneruj: openssl rand -base64 32
GOOGLE_CLIENT_ID=""         # z Google Cloud Console → Credentials → OAuth 2.0
GOOGLE_CLIENT_SECRET=""

# Iteracja 5 (MapLibre) — w zależności od wybranego źródła kafelków
# NEXT_PUBLIC_MAPTILER_KEY=""
